import rx.Observable;
import java.util.concurrent.TimeUnit;
import twitter4j.TwitterStream;

import static observables.utils.ObservableUtil.log;

/**
 * Created by Jonathan Brites on 19/11/20rt16.
 */
public class DoNotCommitThis {
    public static void main(String[] args) {


       /* Observable<T> someData = Observable.create(s -> {
            getDataFromServerWithCallback(args, data -> {
                s.onNext(data);
                s.onCompleted();
            });
        });*/


    }
}
